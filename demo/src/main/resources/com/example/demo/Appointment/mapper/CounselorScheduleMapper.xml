<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.Appointment.mapper.CounselorScheduleMapper">

    <!-- 定义 existsByCounselorAndDateAndTimeSlot 查询 -->
    <select id="existsByCounselorAndDateAndTimeSlot" resultType="boolean">
        SELECT EXISTS (
        SELECT 1
        FROM Counselor_Schedule cs
        JOIN Schedule s ON cs.schedule_id = s.schedule_id
        WHERE cs.counselor_id = #{counselorId}
        AND s.date = #{appointmentDate}
        AND s.time_slot = #{timeSlot}
        )
    </select>

    <!-- 可以在此处添加其他SQL映射语句 -->

</mapper>